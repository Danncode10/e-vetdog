# [P0.8] Configure Vercel production deployment and authentication URLs

## Why This Task Mattered
Without correct production URLs in Vercel, Supabase Auth, and Google OAuth, users cannot sign in, confirm emails, or reset passwords on the live site. This task ensures the canonical HTTPS production origin is configured everywhere so auth flows work end-to-end in production.

## What Was Verified
- **Implementation commits**: Four commits (d5ebab4, 0f03c67, a8f8eb5, 381faca) update all Vercel, Supabase, and Google Cloud configuration instructions and `.env.example`
- **Human app check**: Production deployment succeeds; Supabase Site URL and redirect allow list contain the production URLs; Google Cloud has the production JavaScript origin; email, recovery, and Google sign-in work in production

## Human Evidence
User confirmed P0.8 is verified via `/verify-task` — production Vercel deployment works with correct environment variables, Supabase Site URL and redirect allowlist are configured for the production origin, Google Cloud OAuth authorized JavaScript origin includes the production URL, and all auth flows (email confirmation, password recovery, Google sign-in) complete successfully on the live site.

## Result
Pass. All required production origins are configured in Vercel, Supabase Auth, and Google OAuth; documentation is updated; auth flows work in production.