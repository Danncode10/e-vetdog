# [P3.1] Implement encounter, prescription, signing, amendment, and RLS foundations

## Why This Task Mattered
This task builds the core clinical database tables and security framework, ensuring pet medical histories are immutable once signed and that sensitive medical data is isolated strictly to authorized veterinarians, admins, and permitted owners.

## What Was Verified
- Automated Check: `pnpm exec tsc --noEmit` completed successfully with 0 errors.
- Verification Script: Run `verify-p3.1.ts` which inserts a draft encounter, clinical notes, diagnosis, treatment, and prescription, verifies update access, signs the encounter, verifies locking rules, appends an amendment, and cleans up. All checks passed.

## Human Evidence
- [x] Performed `npx tsx scripts/verify-p3.1.ts` → observed all verification checks (draft creation, note updates, locking policy, amendment insertions, and cleanup) pass successfully.

## Result
Pass. The clinical schema and RLS policies are fully applied and verified to function correctly.
