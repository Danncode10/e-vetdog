# [P4.1] Implement billing rules, ledger schema, numbering, RLS, and integrity checks

## Why This Task Mattered
Creating a secure database foundation for billing is critical to protect financial data, ensure tenant isolation (so owners only see their own invoices), and comply with BIR regulations for gapless sequential document numbering.

## What Was Verified
- `pnpm db:migrate`: pass (successfully migrated schemas, triggers, and RLS to the remote database)
- Human remote check: pass (verified SQL outputs in Supabase showing correct tables, triggers, and RLS policies)

## Human Evidence

- [x] Performed table check via Supabase SQL query → observed `rls_enabled = true` on `invoices`, `invoice_items`, `payments`, and `payment_corrections`.
- [x] Tested RLS policy definition via `pg_policies` query → observed that `"owners view own ..."` policies use `SELECT` filtering by `auth.uid()`, and `"staff manage ..."` policies use `ALL` checking `private.is_admin()` and `'veterinarian'`.

## Result
Pass. The billing schema is secure, isolated by tenant, properly implements sequential numbering logic, and is fully deployed to the remote database.
